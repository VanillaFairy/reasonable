#!/usr/bin/env bash
# Bridge: PreToolUse (edit tools + Bash) -> lib/budget.mjs. Fails open if node absent.
DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
command -v node >/dev/null 2>&1 || exit 0
exec node "${DIR}/../lib/budget.mjs" "$@"
