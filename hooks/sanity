#!/usr/bin/env bash
# Bridge: PreToolUse (edit tools) -> lib/sanity.mjs (lintable sanity invariants).
DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
command -v node >/dev/null 2>&1 || exit 0
exec node "${DIR}/../lib/sanity.mjs" "$@"
