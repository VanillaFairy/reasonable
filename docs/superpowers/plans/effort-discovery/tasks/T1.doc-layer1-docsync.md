# T1.doc — Layer 1 doc-sync + version bump

**Spec:** repo invariant #3 (grammar + parser change together) + §6.4 doc-contradiction resolution + the
spec's build-order doc-sync obligation. **Wave:** 1e (after T1.1–T1.5 merge). **Depends on:** all Layer 1.
**Model:** standard.

## Objective

Transcribe Layer 1's landed changes into the design corpus (verifying each against the merged code) and bump
the plugin version once for the layer. Docs + version ONLY — no `lib/`/`test/`/skill-logic code.

## Scope
- **Edit:** `docs/artifacts.md`, `docs/DESIGN.md`, `docs/architecture.md`, `docs/glossary.md`,
  `.claude-plugin/plugin.json`, `README.md`. Possibly `CLAUDE.md` + `skills/using-reasonable/SKILL.md` for the
  doc-contradiction resolution (see below) — those are doc/prose, allowed here.
- **Do NOT edit:** any `lib/`/`test/` file or a skill's operational logic (this is docs + version + the
  single-effort-vs-multi prose resolution only).

## Method: verify each delta against the MERGED code before writing it. Read `lib/effort.mjs`
(`effortBirthState`, `resolveActiveEffort`, `assertNoAmbiguousBirth`), `lib/reconcile.mjs` (the `lifecycle`
field, the multi-root AMBIGUOUS + S7 born-state HALT buckets, `briefing`), `lib/abandon.mjs` + `lib/ledger.mjs`
(`'abandoned'` event), `lib/session-start.mjs` (the multi-effort briefing), `skills/develop/SKILL.md` +
`skills/analysis/SKILL.md` (config.effort write + gitignore).

## The Layer 1 deltas to document (verify, then transcribe)

1. **`config.effort` birth signature (T1.1)** → `docs/artifacts.md` `config.json` section: document `effort`
   (a non-empty string, the birth signature; written by `develop` Step 0; read by `effortBirthState`,
   `conclude`, `abandon`). Note `effortBirthState`'s four states (absent/corrupt/missing-signature/ok) where
   the config/discovery model is described.
2. **`resolveActiveEffort` discovery + nested layout (T1.2)** → `docs/artifacts.md`/`architecture.md`: the
   `{kind:'resolved'|'none'|'multiple', root?, roots?, strays?, diagnostics?}` result; the canonical nested
   layout `.reasonable-efforts/<name>/.reasonable/` (depth 1); that `strays`/`diagnostics` ride on any kind.
   Resolve the §6.4 **doc contradiction** in favor of multi-effort-nested: `CLAUDE.md:16` ("an effort lives
   in `.reasonable/` at the target project root") and `using-reasonable` (one-at-a-time) vs `analysis`
   (several efforts may share one repo) — reword so multi-effort-nested is the documented model, and document
   the location decision. Keep it light + accurate.
3. **Birth-location policy + multi-root AMBIGUOUS HALT (T1.3)** → `DESIGN.md`/`architecture.md`: the
   birth-location refusal (develop + fence) and the new **">1 `.reasonable`-bearing root reachable" (repo-root
   stray shadowing nested efforts) AMBIGUOUS→HALT class** alongside the existing four in the reconcile
   header / DESIGN §5.12. Don't renumber sections (invariant #4).
4. **reconcile `lifecycle` field (T1.3) + briefing line (T1.5)** → `docs/artifacts.md` reconcile-result /
   briefing shape: `lifecycle ∈ active|at-land-gate|half-concluded` (born states; dir-name states are the
   scan's), surfaced by `briefing()` as a `Lifecycle:` line. "landed" ⟺ effortBranch is an ancestor of base.
5. **`'abandoned'` event + `.reasonable.abandoned-*` archive (T1.4a)** → `docs/artifacts.md` ledger grammar
   (`'abandoned'`, Family 1, `required:[]`, mirrors `'concluded'`) + the `.reasonable.abandoned-<effort>/`
   archive shape; `docs/glossary.md` entry for **`reasonable:abandon`** (the §10 doc-sync obligation names
   "glossary entries touched by reasonable:abandon"); the lifecycle table (§6.5 F10) gains abandon beside
   conclude; note analysis plants `.reasonable.abandoned-*/` in `.gitignore`.
6. **reconcile S7 born-state HALT (T1.5)** → `DESIGN.md`/`architecture.md` §6.1 / §5.12: reconcile's
   config-trust HALT now keys on `effortBirthState` (corrupt **and** missing-signature), beside runMode-absent
   — a born effort with a foreign/hand-edited/torn config HALTs instead of silently proceeding.
7. **Multi-effort SessionStart briefing (T1.5)** → `architecture.md`/`DESIGN.md`: session-start resolves via
   `resolveActiveEffort`, cheaply briefs parallel efforts (per-effort file reads, per-effort try/catch, no
   per-effort reconcile), surfaces strays/parked, flags born-bad configs.

## Version bump (minor: 2.4.0 → 2.5.0)
Layer 1 is a coherent set of backward-compatible new capabilities (deterministic discovery, birth-location,
abandon, lifecycle, multi-effort briefing, born-state HALT). Grep `2.4.0`: `.claude-plugin/plugin.json`,
`README.md` (install snippet + footer `Version:` line). Update all to `2.5.0`. If `marketplace.json` is
external (parent of repo), note it in the report — do NOT reach outside the worktree.

## Constraints
- Do NOT renumber DESIGN/architecture sections (invariant #4 — cited from code). Edit prose within sections.
- Mark machine-parsed artifacts with the `*` convention.
- Match what SHIPPED — follow the code where it differs from a delta above, and note the discrepancy.

## Acceptance
- `docs/artifacts.md` matches the shipped config.effort / resolveActiveEffort result / lifecycle / abandoned
  grammar; DESIGN/architecture updated (birth-location, multi-root + S7 HALT classes, lifecycle, multi-effort
  briefing) without renumbering; glossary has `reasonable:abandon`; the single-vs-multi-effort contradiction
  resolved to multi-nested; version 2.5.0 everywhere; whole suite still green (docs-only). Report the exact
  version-string locations + whether marketplace.json is external.

## Context
This is the single writer on `artifacts.md`/`DESIGN.md`/`glossary.md` for Layer 1. Do it only after T1.1–T1.5
are merged and their reported doc-deltas are in hand. `T0.doc` (the Layer-0 doc-sync) is your structural
template.
