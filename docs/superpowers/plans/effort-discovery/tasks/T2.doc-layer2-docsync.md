# T2.doc — Layer 2 doc-sync + version bump

**Spec:** repo invariant #3 (grammar + parser change together) + spec §10 (doc-sync obligations). **Wave:**
2e (after T2.1–T2.4 merge). **Depends on:** all Layer 2. **Model:** standard.

## Objective

Transcribe Layer 2's landed changes into the design corpus (verifying each against the merged code) and bump
the plugin version once for the layer (2.5.0 → 2.6.0). Docs + version + the one Correction-C prose fix ONLY —
no `lib/`/`test/`/skill-logic behavior change.

## Scope
- **Edit:** `docs/artifacts.md`, `docs/DESIGN.md`, `docs/architecture.md`, `docs/glossary.md`,
  `.claude-plugin/plugin.json`, `README.md`. Plus `agents/route-planner.md` (the Correction-C prose fix only —
  a doc-accuracy correction, allowed here).
- **Do NOT edit:** any `lib/`/`test/` file or a skill's operational logic. Do NOT renumber DESIGN/architecture
  sections (invariant #4). Mark machine-parsed artifacts with `*`.

## Method: verify each delta against the MERGED code before writing it
Read: `lib/route.mjs` (`readRoute` → `{route, diagnostic}`), `lib/next-action.mjs` (`projectDirectives`,
`selfCheckDirectives`), `lib/reconcile.mjs` (the state assembly, the self-check gate, the `next-action`
append, `briefing()`'s NEXT line), `lib/ledger.mjs` (`EVENT_SCHEMAS['next-action']` + `validateNextAction`),
`lib/progress-map.mjs` (`composeNextAction`, the `▶ NEXT` block, `atomicWrite`'s retry, `renderDirectives`),
`lib/redispatch-guard.mjs` (the exported `redispatchBlock`/`hashWorkOrder`), `skills/analysis/SKILL.md` +
`skills/retro/SKILL.md` (route.json persistence), `agents/work-order-writer.md` + `agents/route-planner.md`
(`dependsOn`).

## The Layer 2 deltas to document (verify, then transcribe)

1. **`route.json` (new machine artifact `*`)** → `docs/artifacts.md`: a new section (or beside `route.md`) —
   `{ slices:[sliceId…], ratifiedAt, ledgerSeq }`, single-writer (orchestrator; SEALED-class — no fence
   change), carries only the ratified slice ORDER (WO→slice membership stays on each WO's `verticalSlice`).
   `route.md` is **demoted to human narration, never parsed** — say so. Persisted at `analysis` step 10a AND
   rewritten at `retro` step 5 (kept in sync with `route.md`). Read by `lib/route.mjs readRoute(effortRoot)` →
   `{ route: {slices, ratifiedAt, ledgerSeq}|null, diagnostic: string|null }` (absent → null; malformed → null
   + diagnostic; never fabricates an order).
2. **WO `dependsOn` (required field)** → `docs/artifacts.md` `work-orders/<id>.json` section: add
   `dependsOn: [workOrderId]` ( `[]` = no deps), written once at spec creation by `work-order-writer` from the
   route-planner's proposed readiness edges. Document the **readiness predicate**: a WO is ready iff its status
   is not terminal and every dep's `status === 'done'` — **Correction C** (there is no `green`/`merged` status;
   `merged` folds into `done` in reconcile's `workOrderStatuses`) and **Correction D** (a `node-canceled` dep is
   terminal-abandoned and does NOT satisfy `dependsOn`; canceled is detected via the progress tree, not the
   fold). Distinguish `dependsOn` (readiness/ordering) from the footprint independence edges (wave packing).
3. **`next-action` ledger event (`*`, Family 3)** → `docs/artifacts.md` ledger grammar: add `next-action` to
   the Family-1/2/3 enumeration (Family 3) + a row: `{ type:'next-action', directives:[{kind, slice?,
   workOrders?, workOrder?, detail?}], computedFrom? }`. `directives` optional array (each with a non-empty
   `kind`); `computedFrom` optional positive seq (omitted on an empty-ledger projection). The **fold ignores
   `next-action`** for WO status (it is a projection, never status input) and `EVENT_MAP` maps it to no tree
   op. It is a **recorded projection** — reconcile appends ONE per call.
4. **`progress.json.nextAction` + the `▶ NEXT` block (D19 render)** → `docs/artifacts.md` progress-mirror
   section + `DESIGN.md`/`architecture.md` D19: the mirror re-derives the **latest** `next-action` event on
   every regen into `progress.json.nextAction` (a **string**) + a `▶ NEXT` blockquote atop `progress.md`, with
   the mechanical **staleness** suffix `computed at seq <computedFrom>, <K> event(s) since` (K = non-`next-action`
   events after `computedFrom`; `fresh` at K=0). The directive **survives the wholesale regen by construction**
   (it lives in the ledger, never poked into `progress.json`). Note `atomicWrite`'s EPERM/EBUSY rename retry
   (Windows hardening). `session-start.mjs` + `briefing()` surface it.
5. **The deterministic `nextAction` projection (§7 model)** → `DESIGN.md`/`architecture.md` (§7 cross-refs, do
   NOT renumber): `nextAction` is a **directive SET** (`projectDirectives`, pure, in `lib/next-action.mjs`),
   computed **only inside `reconcile()`** from the reconstructed state (§7.1/§7.3). Document the directive kinds
   (`HALT|AMBIGUOUS|DECIDE|RUNNING|DISPATCH|RETRO|OPEN|LAND|CONCLUDE|DONE`) and the **taxonomy the code
   shipped**: `AMBIGUOUS` = any unsettleable-config halt (reconcile's `ambiguities`, which INCLUDES the S7
   born-state HALT), `HALT` = the floor-integrity unexplained-breach STOP (`halt` with empty `ambiguities`).
   (This refines §7.3's original "HALT(S7)/AMBIGUOUS(>1 root)" labels to match reconcile's realized partition.)
6. **The output self-check (§7.4)** → `DESIGN.md`/`architecture.md`: `selfCheckDirectives` (pure) is the
   verification-trio applied to the projection — it refuses a DISPATCH/RUNNING of a redispatch-guard-flagged WO
   (dead-end / amendment-drop), an OPEN of a retired slice, or a LAND over a non-empty frontier, downgrading
   each to a **DECIDE** (escalates in both modes, matrix S12). It reuses the guard's **exported
   `redispatchBlock(ledger, wo, computeHash)`** (the guard CLI is now a thin wrapper). **Correction F:** it does
   NOT refuse `node-downgraded` — a downgraded WO is the D19 legitimate reopen. Note in the glossary/§ if a
   term is touched.
7. **Correction C prose fix** → `agents/route-planner.md` (~lines 145–146): the stale "terminal work orders are
   `status:'merged'` or `'green'`-with-`merged:true`" prose predates T0.4 (the journal `status` field was
   retired). Correct it to: terminal = `reconcile()`'s `workOrderStatuses[id] === 'done'` (merged folds into
   done). Keep it minimal + accurate.

## Version bump (minor: 2.5.0 → 2.6.0)
Layer 2 is a coherent backward-compatible new capability (the deterministic, self-checking `nextAction`). Grep
`2.5.0`: `.claude-plugin/plugin.json`, `README.md` (install snippet + footer `Version:`). Update all to
`2.6.0`. `marketplace.json` is external (parent of repo) — note it in the report, do NOT edit outside.

## Constraints
- Do NOT renumber DESIGN/architecture sections (invariant #4). Edit prose within sections.
- Mark machine-parsed artifacts `*`. Match what SHIPPED — follow the code where it differs from a delta above,
  and note the discrepancy.
- Whole suite still green (docs-only): `for t in test/*.test.mjs; do node "$t"; done`.

## Acceptance
- `docs/artifacts.md` matches the shipped `route.json` / WO `dependsOn` / `next-action` event /
  `progress.json.nextAction` grammar (each `*` where machine-parsed); DESIGN/architecture updated (the
  projection SET, the AMBIGUOUS/HALT taxonomy, the self-check, D19 render) without renumbering; the
  route-planner "merged/green" prose corrected (Correction C); version 2.6.0 everywhere; whole suite green.
  Report the exact version-string locations, whether marketplace.json is external, and any code/delta
  discrepancy.

## Context
This closes Layer 2 (and the whole effort's code). `T1.doc`/`T0.doc` are your structural template — inspect how
they landed with `git log --oneline -- docs/artifacts.md`. This is the single writer on
`artifacts.md`/`DESIGN.md`/`glossary.md` for Layer 2; do it only after T2.1–T2.4 are merged (they are).
