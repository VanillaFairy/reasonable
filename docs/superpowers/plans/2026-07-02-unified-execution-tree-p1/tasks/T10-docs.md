# Task T10: Docs — artifacts.md vocabulary, DESIGN §D19, glossary

## References
- Read: `../shared/interfaces.md` §2 (the vocabulary IS the content to document), §3
- Read: `docs/superpowers/specs/2026-07-02-unified-execution-tree-design.md` (Decisions,
  Component sections, Enforcement)
- Read: current `docs/artifacts.md` (ledger.jsonl + progress sections), `docs/DESIGN.md` (the
  D19 paragraph — search `D19`), `docs/glossary.md`

## Dependencies
- Depends on: T03b (vocabulary final). Depended on by: T14.

## Scope
**Files:**
- Modify: `docs/artifacts.md`, `docs/DESIGN.md`, `docs/glossary.md`

**BOUNDARY — nothing else.**

⚠ `docs/artifacts.md` carries UNRELATED uncommitted modifications (plan.md Pre-flight). Edit
surgically — only the sections named below; if Pre-flight has not resolved the dirt, STOP.

## Positive Constraints (DO)
- **artifacts.md — `ledger.jsonl` section:** document the three families exactly per
  interfaces §2 (types, required fields, script stamps `seq`/`ts`/`attempt`/resolved `node`,
  the controller as sole write path, `workOrder`→`node` resolution, legacy `action-*` retired
  and folded as notes). Keep the section's `*` machine-parsed marker convention intact.
- **artifacts.md — `progress.json` / `progress.md` section:** rewrite to the §1/§3 shapes —
  generic node tree, five statuses + glyphs, counts, the two presentation exceptions, mirrors
  regenerated by the controller on every append + the (narrowed) PostToolUse hook.
- **artifacts.md:** delete any remaining `action-*` event documentation.
- **DESIGN.md — the D19 passage:** rewrite in place to describe the 2.0 mechanism: agents and
  the orchestrator report through the ledger controller; progress is a full-replay fold
  (EVENT_MAP) into the generic five-status tree; attempts/reopen semantics; NO renumbering of
  any section (invariant 4 — §-references from code must stay valid).
- **glossary.md:** add/adjust normative entries: *node* (dispatchable unit of the execution
  tree), *kind*, *attempt*, *report event*, *ledger controller*. Match the glossary's voice.

## Negative Constraints (DO NOT)
- Do NOT renumber DESIGN sections. Do NOT touch `docs/architecture.md` (dirty; Plan 2 will).
- Do NOT document Plan-2/3 artifacts (`nodes/`, registry, interpreter) as existing — the spec
  is the forward reference, artifacts.md documents what IS on disk after Plan 1.

## Implementation Steps
1. Edit the three files surgically.
2. Verify no stale references: `grep -n "action-started\|action-report\|progress-live" docs/artifacts.md docs/DESIGN.md docs/glossary.md` → no output (outside historical/roadmap notes).
3. Commit:
```bash
git add docs/artifacts.md docs/DESIGN.md docs/glossary.md
git commit -m "docs: 2.0 ledger vocabulary, progress tree format, D19 rewrite, glossary terms

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

## Acceptance Criteria
- [ ] artifacts.md families match interfaces §2 field-for-field
- [ ] DESIGN § numbering unchanged (verify: `git diff docs/DESIGN.md | grep -E "^[-+]#+ "` shows no heading number changes)
- [ ] No stale action-* docs remain
